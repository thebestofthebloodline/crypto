# Assets

Drop the $CRYPTO logo here as `crypto-logo.png` (square 600x600+, PNG/JPG).
In `index.html`, set: `HUB.image = "./assets/crypto-logo.png"`

Before image is set, a serif italic `$` placeholder shows.
